" I generally use neovim, so this configuration is minimal

" Absolute and relative line numbers
set number
set relativenumber

" 4 spaces instead of tab
set tabstop=4
" > and < distance (if you instead set to 0, it will match 'tabstop')
set shiftwidth=4
set expandtab

" Highlighting
syntax on